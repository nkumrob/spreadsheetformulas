# Claude Memories — Spreadsheet Formulas

## 2026-07-08 — Project inception
- Repo initialized with master PRD (`prd.md`) and MVP execution ticket list (`mvp-execution-tickets.md`).
- Product: SEO-driven Excel/Google Sheets formula library growing into an AI spreadsheet assistant.
- Agreed MVP stack: Next.js (App Router, TS) + Tailwind + MDX content (no DB at MVP) + Plausible/PostHog + ConvertKit/Beehiiv + Vercel. Supabase/Postgres and Stripe deferred to the accounts/payments phase.
- Ticket numbering: SF-0XX, grouped into epics E0–E9; milestones M1–M4.
- Key scope decision: MVP includes ONLY basic explainer + basic fixer tools (LLM route with deterministic pre-checks in SF-050). Generator/converter/optimizer are post-MVP — the homepage "Generate a Formula" CTA points to a waitlist page (SF-072).
- Quality bar: every published formula must be verified in real Excel AND Google Sheets files before publishing (PRD §10).
- No code exists yet; next step is SF-001 (project scaffold).

## 2026-07-08 — MVP skeleton built (milestones M1 + most of M2)
- Stack in place: Next.js 14.2 (App Router, TS strict) + Tailwind 3.4 + Vitest + Zod. All 32 routes prerender statically; first-load JS < 100 kB.
- **Design system: "The Modern Ledger"** — user demanded expert-level, non-AI-slop design. Direction: warm paper (#FAF7F1), ink (#1C1A16), ledger green (#166B4A), rust for errors (#B3402E), hairline rules, faint cell-grid backgrounds. Fonts: Instrument Serif (display), Schibsted Grotesk (UI), Spline Sans Mono (formulas) via next/font. Signature elements: FormulaBar (fx glyph + copy) and SpreadsheetTable (real column letters/row numbers, tinted result column). Keep ALL future UI consistent with this — no Inter, no purple gradients.
- Content is typed TS data (content/*.ts) validated by Zod at build time — NOT MDX (deviation from SF-010; structured fields fit the §10 page anatomy better). 12 formula pages + 4 error-fix pages seeded, all formulas verified logically; error fixes live at /errors/[slug], formulas at /formulas/[slug] (lib/paths.ts routes by kind).
- Search is build-time index + client-side scoring (lib/search.ts), tested against PRD §11 queries. Platform preference persists in localStorage (sf-platform). Feedback + copy + search events funnel through lib/analytics.ts (Plausible-ready, no-op until script added).
- Newsletter posts to /api/subscribe; forwards to NEWSLETTER_WEBHOOK_URL env var when set.
- **Port 3000 is occupied by another app on this machine ("NATO Phonetic")** — always use another port (e.g. 4123) for local servers. The user's own `npm run dev` for THIS project therefore lands on port 3001.
- **CRITICAL: never run bare `next build`/`next start` in this repo — always prefix `NEXT_DIST_DIR=.next-verify`.** The user keeps a long-running `next dev` session; bare builds rewrite `.next` under it and 500 every page (this was the "site keeps breaking" incident). next.config.mjs honors NEXT_DIST_DIR; `.next-verify` is gitignored.
- Visual QA done via Playwright screenshots at 1440px and 375px on home, formula, error, search, categories — all clean, no horizontal scroll on mobile.
- Remaining for launch (see mvp-execution-tickets.md): 38 more formula pages, 6 more error pages, templates (SF-041/042/087), explainer/fixer tools (E5), real analytics + newsletter provider wiring, e2e suite (SF-090).

## 2026-07-08 — MVP content-complete + tools + templates + competitive research
- Content: 56 pages total — 46 formula pages + 10 error-fix pages (all PRD §17 launch topics). 34 formula + 6 error pages were authored by 6 parallel subagents against docs/content-authoring-guide.md; all validated by the Zod build gate + 34 unit tests. Formula spot-checks passed (agents produced correct formulas AND arithmetically-correct sample data).
- Tools (E5, no AI/API needed): lib/formula-check.ts = deterministic checker (smart quotes, unbalanced parens incl. quoted-string awareness, VLOOKUP 3-arg detection w/ auto ",FALSE" correction, locale semicolons, ~110-function catalog for #NAME? detection, error-value → /errors/ links). lib/formula-explain.ts = rule-based explainer with curated function guide + top-level arg splitting. Both at /tools/fix-formula and /tools/explain-formula; /tools/ai is the AI-generator waitlist.
- Templates: 5 real .xlsx with working formulas generated by scripts/build_templates.py (openpyxl, installed --user; dates MUST be datetime.date not strings or formulas break). Pages at /templates/[slug] with mini-spreadsheet previews + download tracking.
- Search ranking lesson: with many pages sharing a function, dedicated-tutorial ranking needs (a) summed field weights not max, (b) primary-function boost, (c) seoTitle-starts-with-term boost. See lib/search.ts.
- e2e: scripts/e2e_smoke.py (14 checks — copy button, fixer correction, explainer, downloads, 404, mobile overflow) run via webapp-testing skill's with_server.py on port 4123. Mobile overflow fix pattern: grid/flex items need min-w-0 when children use whitespace-nowrap.
- Competitive research: docs/research-formula-bots.md (July 2026). Key takeaways: Formula Bot pivoted upmarket ($18-149/mo, vacating simple formula help); GPTExcel is the $9 textbox generator; Arcwise abandoned the niche; MS Copilot's own docs disclaim formula accuracy; Exceljet lost ~50% traffic to an AI content clone (text-only content is not a moat). Our moat = visible testing evidence + deterministic pre-checks + sample-data-with-every-answer + Excel-vs-Sheets axis + templates for retention. P0 recs largely already built into the product.
- Still open before real launch: hosting/domain, Plausible/PostHog script + NEWSLETTER_WEBHOOK_URL env, Google Sheets copy-links for templates, remaining §17 authoring polish (4 launch-list topics folded into error pages), Search Console submission.

## 2026-07-08 — Layer-1 formula verification harness (engine execution)
- Every content page now carries an executable `verification` block (lib/schema.ts): raw grids + pinned expected cell values, executed by HyperFormula 3.3 in tests/formula-execution.test.ts with vi.setSystemTime frozen to 2026-07-08. Coverage: 52/56 pages engine-verified; 4 legitimately null (UNIQUE/RANK/TEXTSPLIT engine gaps). Coverage tests make silent gaps impossible: formula-kind pages MUST have a block or use an engine-missing function.
- HyperFormula quirks learned: (1) bare TRUE/FALSE literals are NOT parsed — harness aliases them via namedExpressions [{name:"FALSE",expression:"=FALSE()"}]; symptom is #NAME? swallowed by IFERROR into wrong-looking values. (2) Whole-column refs (A:A) cause #CYCLE! if the formula cell sits in a referenced column — use bounded ranges or another column. (3) 6-arg XLOOKUP with search mode -1 IS supported. (4) Engine-missing: LOOKUP, AVERAGEIFS, TEXTBEFORE/AFTER/TEXTSPLIT, UNIQUE, SORT, RANK, REGEXEXTRACT, QUERY.
- Verification blocks also test commonMistakes claims (naive formula misbehaves AND guarded formula works) — e.g. fix-div0 pins both A2/B2→"#DIV/0!" and the IF guard→"".
- Product surface: formula pages show a green "✓ Engine-verified against the sample data above" badge (FormulaArticle reads formula.verification); /how-we-test explains the methodology with live coverage counts; linked from footer + homepage trust strip.
- tsconfig needed explicit "target": "ES2017" once test files entered the build's type graph (matchAll iteration).
- One verification agent died on a session limit mid-file (errors3.ts) — its errors2.ts work was complete and valid; I finished errors3.ts by hand. Lesson: agent batches should be small enough to survive limits; always check git diff after a dead agent.
- Remaining verification layers (designed, not built): L2 dual-engine cross-check (LibreOffice headless recalc of templates, Google Sheets API ground truth), L4 corpus/property-based testing of the fixer (Enron/EUSES corpora, fast-check).

## 2026-07-08 — Workbook analyzer (client-side upload, PRD Tool 15 lean tier)
- /tools/analyze-workbook: drag-and-drop .xlsx/.csv → SheetJS parses IN THE BROWSER (file never leaves the machine — the privacy differentiator vs Formula Bot) → health report + HyperFormula recompute + test-your-formula-against-your-data.
- lib/workbook-analysis.ts (pure, 11 unit tests): parseWorkbook (needs sheetStubs:true or SheetJS DROPS formula-only cells — openpyxl writes no cached values), analyzeWorkbook (error cells → fix-page links, numbers-as-text, stray spaces, inconsistent-formula detection via row-stripped signatures + 75% dominance rule), recomputeFindings (stale stored values; skips volatile TODAY/NOW, null cached values, stored errors — engine coercion is looser than Excel so it would false-positive "fix" #VALUE! cells), testFormula (scratch cell beyond used range in the chosen sheet's context).
- Caps: 8MB file, 2000 rows × 100 cols per sheet. xlsx + hyperformula load via dynamic import on file select — analyzer page stays <100kB first load.
- e2e now uploads a real template via set_input_files and asserts a computed result (16 checks total). Tests: 100. Pages: 90.
- Product framing settled with user: two correctnesses — OUR content (engine-verified in CI, badge + /how-we-test) vs THEIR formula (mechanical = machine-checkable, intent = only verifiable by showing results on their data). Execution-on-your-data is the core product interaction; the analyzer + future sandbox/generator all follow the loop: run it in front of the user, never just assert.
